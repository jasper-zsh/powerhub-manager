# PowerHub 新协议说明

## 概述

本文档描述了PowerHub项目的新协议规范，移除了对旧协议的兼容性支持，只保留新的ESP32协议实现。

## 主要变更

### 1. 移除向后兼容性
- ❌ 不再支持旧的服务UUID: `5e0b0001-6f72-4761-8e3e-7a1c1b5f9b11`
- ❌ 不再支持旧的16字节telemetry格式
- ❌ 不再支持旧的12字节通知格式
- ✅ 只支持新的服务UUID: `119B5F1B-1C7A-3E8E-6147-672F-0100-0B5E`

### 2. 简化的协议栈
- **单一服务UUID**: `119B5F1B-1C7A-3E8E-6147-672F-0100-0B5E`
- **标准化数据格式**: 统一的8字节和固定长度格式
- **简化的错误处理**: 明确的数据长度要求

## 新协议详情

### BLE服务配置
- **设备名称**: "PowerHub"
- **服务UUID**: `119B5F1B-1C7A-3E8E-6147-672F-0100-0B5E` (唯一支持的服务UUID)
- **连接模式**: 单设备连接
- **MTU支持**: 最高247字节

### GATT特性

#### 1. 通道状态 (UUID: 0xFFF0)
- **属性**: Read + Notify
- **数据格式**: 6字节 `[CH0, CH1, CH2, CH3, CH4, CH5]`
- **范围**: 0-255 per channel
- **通知**: 状态变化时自动推送

#### 2. 通道控制 (UUID: 0xFFF1)
- **属性**: Write + Write No Response
- **数据格式**: `[模式(1)][通道(1)][参数(N字节)]`
- **支持的控制模式**:
  - 0x00: Set - 立即设置
  - 0x01: Fade - 渐变
  - 0x02: Blink - 闪烁
  - 0x03: Strobe - 频闪

#### 3. 电源管理 (UUID: 0xFFF5)
- **属性**: Read + Write + Write No Response + Notify
- **读取格式**: 8字节配置数据
- **写入格式**: 3字节命令 `[命令(1)][参数(2)]`
- **详细说明**:

**读取响应格式 (8字节)**
```
偏移   长度   类型      描述
0      2      int16     高温阈值 (0.01°C)
2      2      int16     恢复阈值 (0.01°C)
4      2      uint16    睡眠电压阈值 (mV)
6      2      uint16    唤醒电压阈值 (mV)
```

**写入命令 (3字节)**
```
[命令(1)][参数(2), int16, 大端序)]
```

**支持的电源命令**:
| 命令 | 名称 | 参数 | 描述 |
|------|------|------|------|
| 0x01 | 设置睡眠阈值 | uint16 (mV) |
| 0x02 | 设置唤醒阈值 | uint16 (mV) |
| 0x03 | 强制睡眠 | - |
| 0x04 | 强制唤醒 | - |
| 0x11 | 设置高温阈值 | int16 (0.01°C) |
| 0x12 | 设置恢复阈值 | int16 (0.01°C) |

#### 4. 实时监控 (UUID: 0xFFF6)
- **属性**: Read + Notify
- **数据格式**: 36字节完整监控数据
- **定时通知**: 根据ADC采样配置定期发送

**监控数据格式 (36字节)**
```
偏移   长度   类型      描述
0      2      uint16    输入电压 (mV)
2      2      int16     电源区域温度 (0.01°C)
4      2      int16     控制区域温度 (0.01°C)
6      4      float     总输入电流 (A, IEEE 754)
10     24     -         6通道电流数据 (6 x float, A)
34     1      uint8     系统状态标志
35     1      -         保留字节
```

### 通道映射
- **通道范围**: 0-5 (对应6个PWM通道)
- **硬件映射**:
  - CH0: GPIO8
  - CH1: GPIO9
  - CH2: GPIO10
  - CH3: GPIO11
  - CH4: GPIO12
  - CH5: GPIO13

## 连接要求

### 设备要求
- **必须广播新服务UUID**: `119B5F1B-1C7A-3E8E-6147-672F-0100-0B5E`
- **设备名称**: 必须为 "PowerHub" (区分大小写)
- **数据格式**: 必须严格按照新协议规范

### 错误处理
```dart
// 数据长度验证
if (data.length != 8) {
  throw Exception('INVALID_DATA: Expected 8 bytes, got ${data.length}');
}
```

## 故障排除

### 连接失败
如果遇到连接问题，检查：
1. **服务UUID**: 确认ESP32广播正确的服务UUID
2. **设备名称**: 确认设备名称为 "PowerHub"
3. **固件版本**: 确保使用支持新协议的固件版本

### 数据错误
如果遇到 `INVALID_DATA` 错误：
1. **检查日志**: 查看原始字节数据
2. **验证长度**: 确认返回8字节数据
3. **字节序**: 确认使用大端序

## 测试验证

运行测试验证新协议实现：
```bash
flutter test test/unit/ble_service_test.dart
flutter test test/unit/telemetry_new_protocol_test.dart
flutter test test/unit/power_management_test.dart
flutter test test/unit/monitoring_data_test.dart
```

## 迁移指南

### 从旧固件迁移
如果您的ESP32设备仍在使用旧协议，需要：
1. 更新固件以支持新的服务UUID
2. 实现新的GATT特征
3. 更新数据格式为8字节和36字节
4. 确保正确的字节序

### 从应用代码迁移
如果您有使用旧API的应用代码，需要：
1. 更新服务UUID引用
2. 移除对16/12字节格式的依赖
3. 使用新的8字节电源管理格式
4. 利用新的监控功能

## 优势

### 简化性
- **单一协议**: 不需要维护多种格式支持
- **清晰错误处理**: 明确的错误信息和要求
- **标准数据格式**: 一致的数据长度和字节序

### 性能
- **减少开销**: 不需要格式检测和转换
- **更快解析**: 直接解析目标格式
- **减少内存**: 不需要缓存多种格式

### 功能性
- **更丰富的功能**: 支持实时监控和分区温度
- **精确控制**: 6通道PWM和高级控制模式
- **电源管理**: 完整的电源保护和管理

新协议提供了更强大和一致的功能集，同时简化了实现和维护。